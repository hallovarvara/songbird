import { IObjectOfStrings } from '@types';

export const DEFAULT_QUESTIONS_COUNT = 6;
export const MAX_ROUND_POINTS = 5;
export const DEFAULT_PROGRESS_JUMP_STEP = 10000;

export const SOUND_PATHS: IObjectOfStrings = {
  success: 'success',
  fail: 'fail',
};
